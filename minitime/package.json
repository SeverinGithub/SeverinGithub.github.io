{
  "name": "minitime",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Minijob Stunden-Tracker mit Excel-Export",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && rm -rf assets vorlage.xlsx && cp -r dist/index.html dist/assets dist/vorlage.xlsx . && echo '\\n✓ Deployed. Commit + push to publish.\\n  Restore dev index.html with: cp index.dev.html index.html'"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  }
}
